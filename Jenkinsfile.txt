pipeline {
  agent any
  stages {
    stage('Production-deployment') {
      steps {
        input message: 'Do you approve deployment?', ok: 'Yes', submitterParameter: 'proceed', submitter: 'user'
        when {
          expression { return "${params.proceed}" == 'user' }
        }
        sh """
          echo "This is ok"
        """
      }
    }
  }
}
